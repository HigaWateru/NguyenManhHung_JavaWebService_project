spring.application.name=project

server.port=8080
spring.profiles.active=dev

spring.servlet.multipart.max-file-size=50MB
spring.servlet.multipart.max-request-size=50MB

jwt.secret=${JWT_SECRET:8uw0rgjisehg0834gshfovaTvogaufgig08euTf5hg9a0whg9hcivbetGRgore0geogejrgd}
jwt.access-token-minutes=30
jwt.refresh-token-days=7

#logging
logging.file.name=logs/project.log
logging.pattern.file=%d{yyyy-MM-dd HH:mm:ss} %-5level [%thread] %logger{36} - %msg%n

#mail
spring.mail.host=${MAIL_HOST:smtp.gmail.com}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=${MAIL_USERNAME:fc.canhcut1@gmail.com}
spring.mail.password=${MAIL_PASSWORD:gfti xymh plhp kilr}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
